package main

func main() {
	// 1.1 enter data ("2d20")
	// 1.2 parse entered data (move to a separate function)
	// 2.1 process data: generate several random numbers (rand.Intn(20)+1 in loop) (func)
	// 2.2 process data: calculate min, max and sum (func)
	// 3. output data
}
